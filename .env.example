# API Configuration
# URL base del backend
VITE_API_URL=http://localhost:3000/api

# Usar datos mock en lugar del backend real
# true = usar datos mock (InMemoryHotelRepository)
# false o no definido = usar HTTP (HttpHotelRepository)
VITE_USE_MOCK=false
